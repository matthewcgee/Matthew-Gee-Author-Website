/bedspace/
/volleyball/
/piedmont-volleyball/

# Acuitas build output. Ignored for a different reason than the apps above:
# those are built by CI and served, while Acuitas was deliberately taken off
# GitHub Pages (see commit 2c1ecea) and its build step removed from the Pages
# workflow. `npm run build` in acuity-app/ writes here, so ignoring it keeps a
# local build from quietly putting the app back online.
/acuity/
