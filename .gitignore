/bedspace/
/volleyball/
/piedmont-volleyball/

# Acuitas build output. Built fresh by the Pages workflow like the apps above,
# so the compiled bundle is never committed. The public build is produced with
# no VITE_FIREBASE_CONFIG, which keeps it local-only and free of credentials.
/acuity/
